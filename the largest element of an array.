#include<stdio.h>
int main()
{
	int n,i,largest,a[30];
	printf("enter the number: ");
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
		scanf("%d",&a[i]);
	}
	largest=a[0];
	for(i=1;i<n;i++)
	{
		if(largest<a[i])
		largest=a[i];
	}
	printf("largest number %d",largest);
	return 0;
}
