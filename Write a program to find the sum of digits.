#include<stdio.h>
int main()
{
	int i,n,sum,digit;
	printf("enter the number");
	scanf("%d",&n);
	while(n>0)
	{
		digit=n%10;
		sum=sum+digit;
		n=n/10;
	}
	printf("sum of digits of a given number=%d",sum,n,digit);
	return 0;
}
